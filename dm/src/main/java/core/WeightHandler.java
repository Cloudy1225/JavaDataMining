package main.java.core;

/**
 * A class implements the <code>WeightHandler</code> interface to indicate
 * to make use of the information provided by instance weights.
 *
 * @author Cloudy1225
 */
public interface WeightHandler {
    // Nothing in here, because this is just used as an indicator.
}
